info:
  name: Atualizar alunos
  type: http
  seq: 4

http:
  method: PUT
  url: https://fantastic-disco-r4jrqgwvxw4x2xxwx-3000.app.github.dev/alunos/4
  body:
    type: json
    data: |-
      {
        "cidade": "Montes Claros"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("não deve retornar senhaHash", function() {
          expect(res.getBody()).to.not.have.property('senhaHash');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
