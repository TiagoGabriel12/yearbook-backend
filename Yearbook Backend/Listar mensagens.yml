info:
  name: " Listar mensagens"
  type: http
  seq: 8

http:
  method: GET
  url: https://fantastic-disco-r4jrqgwvxw4x2xxwx-3000.app.github.dev/mensagem
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("deve retornar um array", function() {
          expect(res.getBody()).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
